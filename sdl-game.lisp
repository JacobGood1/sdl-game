;;;; sdl-game.lisp

(in-package #:sdl-game)

;;; "sdl-game" goes here. Hacks and glory await!







;(defun main
;    ()
;  (init "pixel pirates" 640 480)
;  (loop
;     while (eq t *running?*)
;     do (progn
;	  (handle-events)
;	  (update)
;	  (render)))
; (clean))


