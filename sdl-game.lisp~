;;;; sdl-game.lisp

(in-package #:sdl-game)

;;; "sdl-game" goes here. Hacks and glory await!

(defconstant game (make-instance 'game :title "" :screen-width 100 :screen-height 200)
  )





;(defun main
;    ()
;  (init "pixel pirates" 640 480)
;  (loop
;     while (eq t *running?*)
;     do (progn
;	  (handle-events)
;	  (update)
;	  (render)))
; (clean))


